Time-Off Microservice — coverage (npm run test:cov)

Last run (combined unit + e2e via jest.full.config.json) — numbers taken from the Jest table row **All files**:

- Statements: 84.03%
- Branches: 45.68%
- Functions: 95.12%
- Lines: 91.13%

Regenerate before submit: from repo root run `npm run test:cov`, then copy the **All files** row (Statements / Branch / Funcs / Lines) into this file so proof matches the command output.

Notes:
- Suites include e2e (`test/*.e2e-spec.js`), service/repo/filter unit specs under `src/`, and mock HCM tests.
- Branch % is pulled down by Nest `*.module.js`, DTO decorators, and some glue code; domain services/repos are tested more heavily (see HTML report under `coverage/`).
